services:
  back:
    build: ./back
    ports:
      - "8000:8000"

  front:
    build: ./front
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    depends_on:
      - back
